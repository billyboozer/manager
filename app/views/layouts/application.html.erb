<!DOCTYPE html>
<html>
<head>
  <title>The Widget Manager Application</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <div class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <div class="brand"><%= image_tag "cogs.png" %> The Widget Manager</div>
            <ul class="nav" style="margin-top: 10px;">
              <li><%= link_to 'Inventory', widgets_path %></li>
              <li><%= link_to 'Boxes', boxes_path %></li>
              <li><%= link_to 'Orders', orders_path %></li>
            </ul>

            <form style="margin-top: 15px;" class="navbar-search pull-right" action="/widgets/filter" method="get">
              <input type="text" class="search-query" name="size" placeholder="Filter by Size or Box Material">
            </form>
        </div>
      </div>
    </div>
    <div class="container" style="margin-top:60px;padding-bottom:40px;">
      <% if current_user %>
        <%= link_to "Sign Out #{current_user.email}", destroy_user_session_path, method: 'delete', class: "btn btn-small pull-right", style: "margin-top:40px;" %>
        <% end %>
      <% if flash[:alert] %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
